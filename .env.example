# WeOnline runs fully standalone on a local IndexedDB backend — no environment
# variables are required to run the app locally (npm run dev).
#
# This file is intentionally left as a placeholder. Add any future config here
# (e.g. a backend API URL) and copy it to .env.local, which is gitignored.
